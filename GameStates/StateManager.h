#ifndef STATEMANAGER_H
#define STATEMANAGER_H

#include "MenuState.h"
#include "GameState.h"

class StateManager
{
private:
  //GameState game;
  //MenuState menu;

public:
  //StateManager();
  //void update(sf::Keyboard::Key&);
  //void drawState(sf::RenderWindow&);
};
#endif
